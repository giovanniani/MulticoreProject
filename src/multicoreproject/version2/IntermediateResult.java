package multicoreproject.version2;

class IntermediateResult {
	public int currentCount;
	public int totalPopulation;

	public IntermediateResult(int count, int total) {
		currentCount = count;
		totalPopulation = total;
	}
}
